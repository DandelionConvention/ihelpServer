eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  redis:
    database: 1
    host: localhost
    password: Test!234
    port: 6379
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: ihuzhu
    url: jdbc:mysql://localhost:33060/ihuzhu?characterEncoding=UTF8&autoReconnect=true&useSSL=false
    username: ihuzhu
logging:
  level:
    com:
      services:
        products:
          ihelp:
            dao:
              mapper: trace
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
base:
  ossDomain: https://ihuzhu-dev.oss-cn-beijing.aliyuncs.com/
